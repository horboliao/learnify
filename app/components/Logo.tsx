import React from 'react';
import Image from 'next/image'
const Logo = () => {
    return (
        // <Image src={`/logo.svg`} alt={'logo'} width="36" height="36" />
        <h1>📚</h1>
    );
};

export default Logo;